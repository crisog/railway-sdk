# Get a plugin by ID.
query plugin($id: String!) {
  plugin(id: $id) {
    __typename
    # containers
    createdAt
    deletedAt
    deprecatedAt
    friendlyName
    id
    logsEnabled
    migrationDatabaseServiceId
    name
    # project
    status
    # variables
  }
}
