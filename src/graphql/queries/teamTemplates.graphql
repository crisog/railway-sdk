# Get all templates for a team.
query teamTemplates($after: String, $before: String, $first: Int, $last: Int, $teamId: String!) {
  teamTemplates(
    after: $after
    before: $before
    first: $first
    last: $last
    teamId: $teamId
  ) {
    __typename
    # edges
    # pageInfo
  }
}
