# Domain with status
query domainStatus($id: String!, $projectId: String!) {
  domainStatus(id: $id, projectId: $projectId) {
    __typename
    cdnProvider
    certificateStatus
    # certificates
    # dnsRecords
    # domain
  }
}
