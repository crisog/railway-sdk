# Get a single environment patch by ID
query environmentPatch($id: String!) {
  environmentPatch(id: $id) {
    __typename
    appliedAt
    # appliedBy
    createdAt
    environmentId
    id
    lastAppliedError
    message
    status
    updatedAt
    # environment
  }
}
